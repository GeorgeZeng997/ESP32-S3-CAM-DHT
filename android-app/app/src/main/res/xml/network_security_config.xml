<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow cleartext HTTP for all (we only talk to LAN devices) -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
